# APVO Frontend

Vite + React frontend for the APVO audio library.

## Development

```bash
npm install
npm run dev
```

The dev server proxies `/api` requests to `http://127.0.0.1:8000`. You can override the API base URL with `VITE_API_BASE_URL`.
